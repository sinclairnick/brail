import { Column, Row, Typography } from '@brail/react';

export const Signature = () => {
  return (
    <Row paddingTop={16} paddingBottom={16}>
      <Column>
        <Typography variant="h4">Thanks,</Typography>
        <Typography variant="h4">The Brail Team</Typography>
      </Column>
    </Row>
  );
};
