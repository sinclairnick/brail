{
  "name": "brail",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build:libs": "pnpm run --parallel --filter \"{apps}^...\" build",
    "dev": "pnpm build:libs && pnpm run --parallel --filter {apps} dev"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@swc/cli": "^0.1.55",
    "@swc/core": "^1.2.152",
    "@types/react": "^17.0.40",
    "chokidar": "^3.5.3",
    "concurrently": "^7.0.0",
    "esbuild": "^0.14.25",
    "lerna": "^4.0.0",
    "tslib": "^2.3.1"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
