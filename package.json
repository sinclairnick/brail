{
  "name": "brail",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:force": "turbo run build --force",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev": "turbo run dev --parallel",
    "publish": "turbo run publish-pkg"
  },
  "devDependencies": {
    "@changesets/cli": "^2.25.0",
    "@types/jest": "^27.4.1",
    "@types/lodash": "^4.14.179",
    "jest": "^27.5.1",
    "npm-run-all": "^4.1.5",
    "prettier": "^2.5.1",
    "rimraf": "^3.0.2",
    "turbo": "^1.5.6",
    "typescript": "^4.8.4"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@6.25.1",
  "dependencies": {
    "ts-jest": "^29.0.3"
  }
}
